syntax = "proto3";

package ip;

option go_package = "github.com/accretional/proto-ip/pb";

message SubNet {
  oneof format {
    string text = 1;
    sint32 prefix_length = 2;
    // TODO add more
  }
}
